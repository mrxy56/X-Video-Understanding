import './VideoModule.css';

export const VideoModule = ({name}) => {
  return (
    <>
      <span>{name}</span>
      <div className="video-module">
        <svg>
        </svg>
      </div>
    </>
  )
}
